import 'package:flutter/material.dart';

class GliderList extends StatefulWidget {
  @override
  _GliderListState createState() => _GliderListState();
}

class _GliderListState extends State<GliderList> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
